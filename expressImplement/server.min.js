const mongoose=require("mongoose"),dotenv=require("dotenv"),app=require("./app");dotenv.config({path:"./config.env"});const DB=process.env.DATABASE.replace("<password>",process.env.DATABASE_PASSWORD);mongoose.connect(DB,{useNewUrlParser:!0,useCreateIndex:!0,useFindAndModify:!1}).then(()=>console.log("DB connection is Sucessful!"));const tourSchema=new mongoose.Schema({name:{type:String,require:[!0,"A tour must have a name"]},rating:Number,price:{type:Number,require:!0}}),port=process.env.PORT||3e3;app.listen(port,()=>{console.log(`App running on port ${port}`)});